import React from 'react'

export default function Images(){
  return (
    <div>
      <h2>Image Gallery</h2>
      <p>Gallery placeholder — will fetch /api/v1/images in next iteration.</p>
    </div>
  )
}